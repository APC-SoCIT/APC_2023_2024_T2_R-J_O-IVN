import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ruth.and.jerry.customer',
  appName: 'ruth-and-jerry-customer',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
